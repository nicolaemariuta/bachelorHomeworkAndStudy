clear all;
close all;
omega = sym('pi')/40;
N = 81;
t = 0:N-1;
s1 = sin(omega * t);
plot(t, s1);
s2 = sin(2 * omega * t);
plot(t, s2);
s3 = sin(3 * omega * t);
plot(t, s3);

s4 = sin(4 * omega * t);
subplot(5, 1, 4), plot(t, s4);
s5 = sin(5 * omega * t);
plot(t, s5);

